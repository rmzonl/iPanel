[Unit]
Description=iPanel Agent Daemon
Documentation=https://github.com/rmzonl/ipanel
After=network.target
Wants=network.target

[Service]
Type=simple
User=root
Group=root
ExecStart=/usr/bin/php /usr/local/ipanel/agent/agent.php
ExecStartPost=/bin/bash -c 'sleep 0.5; restorecon /run/ipanel/agent.sock 2>/dev/null || true'
ExecReload=/bin/kill -HUP $MAINPID
Restart=always
RestartSec=5
StandardOutput=journal
StandardError=journal
SyslogIdentifier=ipanel-agent

# Hardening
NoNewPrivileges=false
PrivateTmp=true
ProtectSystem=false
ProtectHome=false

[Install]
WantedBy=multi-user.target
